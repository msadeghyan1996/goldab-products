@extends('layouts.app') @section('title',$product->title) @section('page-heading','جزئیات محصول')
@section('content')
<div class="d-flex flex-wrap gap-2 justify-content-between align-items-center mb-4"><div><h1 class="h4 mb-1">{{ $product->title }}</h1><code>{{ $product->code }}</code></div><div><a class="btn btn-outline-primary" href="{{ route('products.edit',$product) }}"><i class="bi bi-pencil ms-1"></i>ویرایش</a> <a class="btn btn-light" href="{{ route('products.index') }}">بازگشت</a></div></div>
<div class="row g-4"><div class="col-lg-5"><div class="card"><div class="card-body">@if($product->main_image)<img class="img-fluid rounded w-100 mb-3" style="max-height:420px;object-fit:contain" src="{{ '/storage/'.ltrim($product->main_image, '/') }}" alt="{{ $product->title }}">@else<div class="bg-light rounded text-center py-5 text-muted"><i class="bi bi-image display-3"></i><p>بدون تصویر اصلی</p></div>@endif @if($product->images->isNotEmpty())<div class="row g-2">@foreach($product->images as $image)<div class="col-4"><img class="img-fluid rounded" style="aspect-ratio:1;object-fit:cover" src="{{ '/storage/'.ltrim($image->path, '/') }}" alt=""></div>@endforeach</div>@endif</div></div></div>
<div class="col-lg-7"><div class="card h-100"><div class="card-body p-4"><h2 class="h6 mb-4">مشخصات محصول</h2><dl class="row border-bottom pb-3"><dt class="col-sm-4 text-muted">دسته‌بندی</dt><dd class="col-sm-8">{{ $product->category->name }}</dd><dt class="col-sm-4 text-muted">وضعیت</dt><dd class="col-sm-8"><span class="badge bg-{{ $product->availability === 'available' ? 'success' : 'secondary' }}">{{ $product->availability === 'available' ? 'موجود' : 'ناموجود' }}</span></dd><dt class="col-sm-4 text-muted">وزن</dt><dd class="col-sm-8">{{ $product->weight ? number_format((float)$product->weight,3).' '.$product->weight_unit : '—' }}</dd><dt class="col-sm-4 text-muted">اجرت درصدی</dt><dd class="col-sm-8">{{ $product->wage_percentage !== null ? $product->wage_percentage.'٪' : '—' }}</dd></dl>@if($product->short_description)<h3 class="h6 mt-4">توضیحات کوتاه</h3><p>{{ $product->short_description }}</p>@endif</div></div></div></div>
@endsection
